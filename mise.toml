[env]
_.aws-ssm = {
    tools = true,
    parameters = {
        TF_VAR_github_client_id = "/gh-agent/gh-oauth-client-id",
        TF_VAR_github_client_secret = "/gh-agent/gh-oauth-client-secret"
        }
    }
